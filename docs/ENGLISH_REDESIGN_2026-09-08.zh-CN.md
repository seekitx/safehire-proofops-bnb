# 英文界面与暖白深绿重设计

用户要求网站不出现中文，并明确选择 baoyu-design 的暖白加深绿方向。设计稿位于 `designs/safehire-marketplace-refresh/`，未放入忽略规则。

## 实现范围

- 统一现有 11 个页面的排版、导航、按钮、表单、状态和移动端样式。工作台重新安排为选择服务、保存订单、查看结果三个区域。
- 卡片集中显示费用、范围和当前报价状态；不把签名样例通过写成实际购买成功。完整限制可展开查看。
- 替换 411 项原中文界面、错误提示、行动准备和 Bark 文案，日期明确使用英文地区格式。
- 对照实验界面使用独立英文题目译本；原始中文任务快照及原始人工记录不改，下载记录另记实际展示的英文题目，避免改写历史实验。
- 证据页将本人评价标为英文翻译；原始证据文件、历史视频与正式提交回执保持原样。研究文档可继续使用中文，网站产品界面使用英文。
- 钱包逐步确认、签名校验、价格上限、提醒授权和订单状态转换没有放宽。

## 检查

- Python 417 项测试通过；类型检查、代码检查、安全扫描通过。既有通知测试同步更新英文验证码前缀。
- 11 个页面分别以桌面 1440px 和手机 390px 检查，全部返回成功，无页面脚本错误、横向溢出或可见中文。四类切换均检查。此检查不等于重新执行钱包付款。
- 修复旧全宽输入样式导致手机购买页的确认框挤压说明文字的问题。
- 设计预览使用实际界面快照，明确标记不是当前报价；真实购买与监控以线上工作台为准。

## 视频

`scripts/refresh_demo_visuals.cjs` 将重新录制实际公开页面，不注入钱包、不模拟交易、不伪造接口响应。保持已有 MiniMax 配音和 52 条字幕的时间，替换七段网站画面。最终上线与媒体文件校验记录在后续追加。

### 发布候选验证

- 网站候选 `bbe5337` 的 GitHub 三组自动检查通过（运行 34231315592）。绿云候选 355 个打包文件及五个实际安装的 Python 模块均与源文件一致，健康接口和新页面资源返回成功。
- 使用独立本地测试数据库实际验证创建工作台、保存价格范围监控、暂停、刷新恢复、分类对应表单，以及英文题目展示和自动练习记录归类。测试记录明确为 `ui_verification_not_human`，不计入比赛真人对照，也没有发出手机提醒或链上交易。

### 线上页面与最终视频

- 网站版本 `bbe5337` 已在绿云运行且健康；线上样式文件完整下载与本地一致。九个公开页面返回成功、无可见中文和页面脚本错误；两个开发工具页在生产环境按原配置关闭，未为展示开放。
- 七段画面均从真实公开网站重新录制。网格片段取得真实 `SIGNED` 报价；各分类页面文字检查未发现汉字。未注入钱包、重复付款或模拟服务器响应。
- 最终视频 138.041333 秒；独立提取 AAC 音频后摘要与改版前完全一致，52 条字幕文件保持不变。抽查市场和交付画面，字幕完整显示在专用底栏。
- 当前视频摘要与录制页面、时间、源版本见公开 `visual-refresh-manifest.json`。原始提交回执未修改；原始提交视频保留，旧配音版另有 Git 历史和本地备份。
- 加入资源版本参数，避免已打开过旧站点的浏览器沿用中文脚本或旧样式。首页不再使用可能过时的固定代理数量标题。

### 实际工作台验收发现的交付定位问题

在宿主机 Chrome 中打开原有私有工作台后，#56743 的历史交付仍在，但最新跟进报 `eth_getLogs` 来源不可用。配置只为 #56741 保存交付交易定位提示，#56743 仍需反复扫描日志。

已在绿云通过现有严格校验器重新读取 #56743 的真实交付交易 `0x7959d1eabce11be788978e5f52d00ca6cf8f43fce641b083e13004b2eaddcdb9`：交易成功、区块 120675335 属于当前规范链、事件对应正确政策合约和订单，交付哈希与当前订单一致；当前状态为 SUBMITTED。只补充经过核实的交易定位提示，不缓存成功结论、不绕过任何校验、不执行结算。后续仍逐次核对当前链、订单与下载的原始交付。
