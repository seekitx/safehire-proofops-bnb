services:
  safehire:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - safehire-data:/app/.data
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
volumes:
  safehire-data:
